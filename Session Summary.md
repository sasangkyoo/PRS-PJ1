# 테스트 산출물 자동 생성 시스템 - 개발 진화 요약 리포트

본 문서는 프로젝트의 초기 프로토타입(1안)부터 안정성 확보(2안), 유지보수성 및 개발자 경험 고도화(3안)에 이르기까지의 발전 과정을 하나의 흐름으로 요약한 것입니다.

---

## 🌊 단계별 진화 흐름 요약

### 🚀 1단계 (수정안 1번): 핵심 성능 혁신 및 AI 품질 확보 (프로토타입 ➔ 고성능 시스템)
초기 단계에서는 시스템의 **근본적인 처리 속도와 결과물의 품질(정확도)을 끌어올리는 데 집중**했습니다.
* **압도적인 속도 향상:** 순차 처리 방식에서 `asyncio`를 활용한 비동기 병렬 처리로 전환하여, 수십 장의 문서를 한 장 처리하는 시간과 비슷하게 단축했습니다.
* **파싱 에러 원천 차단:** OpenAI의 `Structured Outputs (Pydantic)`을 강제 적용하여, 일관된 JSON 데이터만 응답받도록 강제해 파싱 에러율을 0%로 만들었습니다.
* **환각(Hallucination) 억제:** 엄격한 프롬프트 엔지니어링과 Pydantic 필드 설명을 통해 AI가 없는 UI를 지어내는 것을 막고, 1:N 테스트 케이스(정상/예외/경계값) 도출을 의무화했습니다.
* **AI 기반 중복 제거:** `gpt-4o-mini`를 도입해 단순히 텍스트가 아닌 '의미론적'으로 같은 기능을 찾아 그룹화하고, 종속된 하위 기능을 자동으로 다시 연결(Auto-Reparenting)하는 고도화된 UI/UX 로직을 구현했습니다.

### 🛡️ 2단계 (수정안 2번): 안정성, 예외 처리, 코드 모듈화 (고성능 ➔ 엔터프라이즈급 안정성)
1단계에서 확보한 성능을 기반으로, 대규모 요청 시 발생할 수 있는 **병목 현상과 엣지 케이스를 방어하고 코드 아키텍처를 정돈**했습니다.
* **API Rate Limit 방어:** 비동기 요청이 한 번에 너무 많이 몰려 발생하는 `429 에러`를 막기 위해 Semaphore(동시 실행 수 제어)와 지수 백오프 기반의 자동 재시도(`tenacity`)를 도입했습니다.
* **비용 및 지연 시간 최적화:** 무거운 고해상도 이미지를 그대로 LLM에 보내는 대신, 비율을 유지하며 적절히 리사이징(최대 2000px)한 후 전송하여 토큰 낭비와 응답 속도를 개선했습니다.
* **엣지 케이스 완벽 대응:** 특정 페이지 변환이 실패하더라도 시스템이 멈추지 않게 Dummy 객체를 반환하게 하고, 부모 기능이 삭제되었을 때 자식 기능들의 식별자가 꼬이는(Dangling Child) 현상을 방어했습니다.
* **관심사 분리(리팩토링):** 거대해진 `llm_service.py`에서 스키마(`schemas.py`)와 프롬프트(`prompts.py`)를 분리해 내어 코드 가독성과 유지보수성을 크게 높였습니다.

### 🛠️ 3단계 (수정안 3번): 유지보수성 및 개발 환경 고도화 (안정성 ➔ 지속 가능한 시스템)
마지막 단계에서는 코드를 처음 보는 사람이나 미래의 유지보수를 위해 **개발자 경험(DX)과 디버깅 환경을 개선**하는 데 주력했습니다.
* **상태 관리 직관성 확보:** UI 단계를 나타내는 하드코딩된 숫자(1, 2, 3...) 대신 `Enum` 클래스(`AppStep` - `UPLOAD`, `SELECT` 등)를 도입하여 코드 흐름을 명확하게 만들었습니다.
* **백그라운드 오류 추적:** Streamlit UI 에러 외에도 시스템 내부 깊숙한 예외 사항(Stack Trace)을 기록하기 위해 `logging` 모듈을 연동하고 `app.log` 파일에 상세 로그를 남기도록 체계를 구축했습니다.
* **타입 안정성 보장:** `typing` 모듈을 활용한 타입 힌트를 핵심 함수 전반에 적용하여, IDE의 자동완성 지원을 강화하고 개발 중 발생할 수 있는 휴먼 에러를 미연에 방지했습니다.

---

## 💡 총평
이 프로젝트는 **1) 기능 작동과 속도(비동기/Pydantic) ➔ 2) 예외 상황에 대한 견고함(Rate Limit/리사이징) ➔ 3) 코드의 질과 유지보수(Enum/Logging/Type Hint)** 라는 매우 이상적인 소프트웨어 개발 방법론을 따라 발전했습니다. 현재는 대규모 문서도 빠르고 안정적으로 처리할 수 있으며, 향후 확장이나 유지보수도 용이한 탄탄한 기반을 갖추게 되었습니다.

---

## 🚀 향후 시스템 고도화 방향성 제안 (Next Steps)

현재 완성된 시스템 아키텍처를 기반으로, 진정한 **'엔터프라이즈 프로덕션(Production-ready)' 레벨로 고도화**하기 위한 4가지 핵심 방향성을 제안합니다.

### 1. UX 및 안정성 고도화: 상태 영속성(Persistence) 확보
* **이슈:** Streamlit의 특성상 `st.session_state`에 의존하고 있어, 브라우저 새로고침(F5)을 누르거나 세션이 만료되면 중간 작업 내역(이미지, 추출된 기능 정의서 등)이 모두 유실될 위험이 있습니다.
* **해결 방안:** `session_id`를 기반으로 중간 결과물을 `session_dir` 내에 실시간 파일(JSON 등)로 저장(Checkpoint)하는 기능을 추가합니다. 앱 재접속 시 활성화된 세션 파일이 있다면 "이전 작업 내역을 불러오시겠습니까?"를 묻는 복구 로직을 구현하여 사용자 경험을 극대화할 수 있습니다.

### 2. 코드 아키텍처 고도화: 비즈니스 로직 완전 분리 (Separation of Concerns)
* **이슈:** `app.py` 내부(특히 Step 4)에 Auto-Reparenting 로직, DataFrame 데이터 변환, JSON 그룹화 등 무거운 비즈니스 로직이 UI 렌더링 코드와 혼재되어 파일이 방대해지고 있습니다.
* **해결 방안:** UI(`app.py`)는 화면을 그리고 입력을 받는 역할만 하도록 책임을 축소합니다. `data_transformer.py` 또는 `business_logic.py` 같은 헬퍼 모듈을 신설하여, DataFrame 변환 로직이나 부모 잃은 자식(Dangling child) 처리 로직을 외부 함수로 분리하면 향후 유지보수가 훨씬 수월해집니다.

### 3. AI 품질 고도화: Vector DB 및 RAG 도입 (확장성 대비)
* **이슈:** 모든 슬라이드를 분석할 때 `common_policy` 텍스트 전체를 프롬프트에 반복해서 주입하고 있습니다. 정책서가 수십 페이지에 달할 경우 토큰 낭비와 응답 지연이 발생합니다.
* **해결 방안:** 추출된 공통 정책을 문단 단위로 쪼개어 가벼운 Vector DB(예: ChromaDB, FAISS)에 저장하는 RAG(Retrieval-Augmented Generation) 방식을 도입합니다. 개별 화면 분석 시 LLM이 해당 화면과 관련된 정책만 검색하여 참고하도록 변경하면 처리 속도 상승과 막대한 API 비용 절감을 이룰 수 있습니다.

### 4. 워크플로우 고도화: 테스트 케이스 'Human-in-the-loop (HITL)' 편집기 추가
* **이슈:** 기능 정의서는 표(Data Editor)를 통해 꼼꼼하게 검수 및 수정이 가능하지만, 최종 산출물인 '테스트 케이스'는 AI가 생성한 후 바로 엑셀로 다운로드해야 하므로 UI 상에서 즉각적인 수정이 불가능합니다.
* **해결 방안:** UI에 `Step 5: 테스트 케이스 검수` 단계를 새롭게 추가합니다. 기능 정의서와 마찬가지로 Data Editor에 생성된 테스트 케이스 목록(사전 조건, 테스트 스텝, 기대 결과 등)을 띄워 사용자가 직접 수정할 수 있게 한 뒤, 최종적으로 엑셀을 다운로드하도록 워크플로우를 보강합니다.
